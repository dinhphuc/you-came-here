name: someone-stars-repository
concurrency:
  group: product
  cancel-in-progress: true
  
on:
  workflow_dispatch:
  watch:
    types: [started]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 12.21.0
    - run: npm ci
    - name: Someone has star
      run: npm run generate
    - name: Update README.md
      run: |
        git config --global user.email "minhphucpk.1997@gmail.com"
        git config --global user.name "dinhphuc"
        git add .
        git commit -m "Someone has star -> Updated README.md" || echo "Someone has star"
        git push
